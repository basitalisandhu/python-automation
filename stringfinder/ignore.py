
                    if any(f.endswith('.txt') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.png') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.exe') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.jpg') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.log') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.pdf') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.zip') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.svg') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.dat') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.tmp') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.jpeg') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.css') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.js') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.html') for f in files):
                        continue  # ignore this directory
                    if any(f.endswith('.json') for f in files):
                        continue  # ignore this directory
                    # if any(f.endswith('.php') for f in files) or any(f.endswith('.py') for f in files) :
                    #     continue  # ignore this directory
                    if any(f.endswith('.asp') for f in files) or any(f.endswith('.lasso') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.aspx') for f in files) or any(f.endswith('.lasso') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.gif') for f in files) or any(f.endswith('.cfm') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.pl') for f in files) or any(f.endswith('.htc') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.pkc') for f in files) or any(f.endswith('.xml') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.bash_profile') for f in files) or any(f.endswith('.desktop') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.el') for f in files) or any(f.endswith('.md') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.db') for f in files) or any(f.endswith('.sql') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.xls') for f in files) or any(f.endswith('.icc') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.swf') for f in files) or any(f.endswith('.ttf') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('README') for f in files) or any(f.endswith('.csv') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.mno') for f in files) or any(f.endswith('.ttf') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('README') for f in files) or any(f.endswith('.csv') for f in files) :
                        continue  # ignore this directory
                    if any(f.endswith('.scss') for f in files) or any(f.endswith('.flv') for f in files) :
                        continue  # ignore this directory
                   